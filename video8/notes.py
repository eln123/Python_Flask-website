# video 8 is about Adding, Deleting, and Updating users with SQLALchemy


# ADD
    # db.session.add(usr)
    #        
    #         db.session.commit()


# DELETE
# found_user = users.query.filter_by(name = user).delete()
# db.commit()